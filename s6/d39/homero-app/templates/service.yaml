apiVersion: v1
kind: Service
metadata:
  name: {{ include "homero-app.fullname" . }}
  labels:
    {{- include "homero-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homero-app.selectorLabels" . | nindent 4 }}
